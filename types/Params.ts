// Define the type for UserProfileParams
interface Params {
  id?: string // Optional user ID
  search?: string // Optional search query
  name?: string // Optional user name
}

export default Params
